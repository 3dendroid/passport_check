[phases.setup]
aptPkgs = ["chromium", "chromium-driver"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python bot.py"

[nixpacks]
packages = ["chromium", "chromedriver", "google-chrome"]
